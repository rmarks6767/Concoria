enemy_health = argument0;
num_enemy = 0;
//distance to nearest enemy + # of enemies

while (instance_nearest(x, y, Enemy) != dup[i])
{	
	if distance_to_object(instance_id_get(Enemy)) < 100
	{
		for (var i = 0; i < (instance_count-1); i++)
		{
			if instance_nearest(x, y, Enemy) == dup[i]
			{
				dup[i] = instance_nearest(x, y, Enemy);
				num_enemy = 
			}	
		}
	}
	howfar = distance_to_object(i)  
	
	
	
	
	
	
	
	
	
	probability = (abs(enemy_health - player_health))/100 
}






return probability